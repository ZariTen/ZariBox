name: CI

on:
  push:
    branches: [main]
    paths:
      - "zaribox/**/*.py"
      - "tests/**/*.py"
      - pyproject.toml
      - .forgejo/workflows/python-test.yml
  pull_request:
    branches: [main]
    paths:
      - "zaribox/**/*.py"
      - "tests/**/*.py"
      - pyproject.toml
      - .forgejo/workflows/python-test.yml

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: codeberg-small
    timeout-minutes: 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install Ruff
        run: python -m pip install --disable-pip-version-check --no-input ruff

      - name: Check with Ruff
        run: ruff check --select E9,F63,F7,F82 .

  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: codeberg-small
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        # Test every supported Python on PRs; use one representative version on main.
        python-version: ${{ github.event_name == 'pull_request' && fromJSON('["3.10","3.11","3.12","3.13"]') || fromJSON('["3.12"]') }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and test dependencies
        run: python -m pip install --disable-pip-version-check --no-input ".[dev]"

      - name: Run tests
        run: python -m pytest
